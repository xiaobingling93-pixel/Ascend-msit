apiversion: "modelslim_v1"
spec:
  process:
    - type: "dynamic_cache"
      qconfig:
        scope: "per_channel"
        dtype: "int8"
        symmetric: true
        method: "minmax"
      include: [ "*" ]
      exclude: [ "model.layers.0.self_attn" ]

  dataset: test.json